# Task 3 Report: Conversation and Research Workflow UI

## Implementation summary
- Wired `frontend/src/App.tsx` to load conversations and LLM profiles on mount, create/select conversations, start research runs, and resume interrupted runs with the required decision payloads.
- Added `ConversationPanel`, `ResearchWorkspace`, and `PlanPanel` as props-driven components using the existing typed API contracts and Task 2 profile shapes.
- Kept `ProgressStream`, `ReportPanel`, and `SettingsPanel` out of scope, leaving explicit placeholders in the shell where needed.

## TDD evidence
### RED
- Command: `npm.cmd test -- src/components/PlanPanel.test.tsx src/components/ConversationPanel.test.tsx src/components/ResearchWorkspace.test.tsx`
- Result: failed before implementation.
- Evidence:
  - `Failed to resolve import "./PlanPanel" from "src/components/PlanPanel.test.tsx". Does the file exist?`
  - `Failed to resolve import "./ConversationPanel" from "src/components/ConversationPanel.test.tsx". Does the file exist?`
  - `Failed to resolve import "./ResearchWorkspace" from "src/components/ResearchWorkspace.test.tsx". Does the file exist?`

### GREEN
- Command: `npm.cmd test -- src/components/PlanPanel.test.tsx src/components/ConversationPanel.test.tsx src/components/ResearchWorkspace.test.tsx`
- Result: pass.
- Evidence: `Test Files 3 passed` and `Tests 5 passed`

### Build verification
- First build command: `npm.cmd run build`
- First result: failed with `src/App.tsx(69,50): error TS2345: Argument of type 'number | null' is not assignable to parameter of type 'number'.`
- Fix: captured `activeConversationId` into a narrowed local `conversationId` before the async loader call.
- Final build command: `npm.cmd run build`
- Final result: pass with `✓ built in 2.92s`

## Files changed
- `frontend/src/App.tsx`
- `frontend/src/components/ConversationPanel.tsx`
- `frontend/src/components/ResearchWorkspace.tsx`
- `frontend/src/components/PlanPanel.tsx`
- `frontend/src/components/ConversationPanel.test.tsx`
- `frontend/src/components/ResearchWorkspace.test.tsx`
- `frontend/src/components/PlanPanel.test.tsx`

## Self-review
- Used the existing `api` client and exported types instead of introducing new backend contracts.
- Resume decisions match the required payloads exactly for approve and replan flows.
- Tests cover the Task 3 component behaviors called out in the brief, but they do not yet exercise full `App.tsx` integration across async API flows.

## Concerns
- `interrupt_payload` is parsed defensively as `{ summary, options }`, but the backend payload shape is still effectively loose at the type level because `ResearchRunResponse` exposes it as `Record<string, unknown> | null`.
- `frontend/tsconfig.tsbuildinfo` was regenerated by the build and remains outside the scoped file list for this task.
